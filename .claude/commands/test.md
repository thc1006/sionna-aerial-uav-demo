---
description: Run the Python test suite for this project.
argument-hint: "[no args]"
allowed-tools: Bash(pytest:*)
---

Run the tests with:

```bash
pytest -q
```

If tests fail, read the failure messages carefully, identify the root cause,
propose a fix, apply it, and re-run the tests until they pass.
